# Non-negotiable default: the deterministic quant brain must run without the LLM.
LLM_ENABLED=false

# LIVE | PAPER | BACKTEST | REPLAY
RUN_MODE=paper

DATABASE_URL=postgresql+asyncpg://localhost:5432/index_option_brain
REDIS_URL=redis://localhost:6379/0

KILL_SWITCH_ENABLED=false
